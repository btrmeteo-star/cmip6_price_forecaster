name: MLflow Pipeline (Zero-Codespaces)

on:
  push:
    branches: [ main ]
  workflow_dispatch:          # 手动触发按钮

jobs:
  pipeline:
    runs-on: ubuntu-latest    # 免费 GitHub-hosted runner
    steps:
      # ---------- 1. 检出代码 ----------
      - name: Checkout
        uses: actions/checkout@v4

      # ---------- 2. 缓存 pip ----------
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      # ---------- 3. 安装 Python 依赖 ----------
      - name: Install Python packages
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl net-tools
          pip install --upgrade pip
          pip install -r requirements.txt

      # ---------- 4. 生成气象特征（三商品） ----------
      - name: Generate features (rice/corn/barley)
        run: |
          for crop in rice corn barley; do
            echo "=== 特征 $crop ==="
            python -m cmip6_commodity.${crop}_processor
            echo "=== 训练 $crop ==="
            COMMODITY=$crop python train.py
          done

      # ---------- 5. 启动 MLflow UI 并等待端口就绪 ----------
      - name: Start MLflow UI & wait port
        run: |
          nohup mlflow ui --host 0.0.0.0 --port 8000 > mlflow.log 2>&1 &
          echo "⏳ 等待 MLflow 端口 8000 就绪..."
          for i in {1..60}; do
            if curl -f http://localhost:8000 > /dev/null 2>&1; then
              echo "✅ MLflow UI 已就绪"
              break
            fi
            echo "⏳ 第 $i/60 秒重试..."
            sleep 1
          done
          if ! curl -f http://localhost:8000 > /dev/null 2>&1; then
            echo "❌ 无法连接，查看日志："
            cat mlflow.log
            exit 1
          fi

      # ---------- 6. 输出本次最佳模型信息 ----------
      - name: Print best models
        run: |
          echo "=== 本次最佳模型 ==="
          for crop in rice corn barley; do
            r2=$(grep -oP "R²=\K[0-9.]+" mlruns/*/$(ls -t mlruns/*/meta.yaml | head -1 | xargs grep -l "${crop}")/../metrics/r2 2>/dev/null || echo "0")
            echo "${crop}-price-model  R²=${r2}"
          done

      # ---------- 7. 上传 artifact（可选，保留模型与日志） ----------
      - name: Upload MLflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts
          path: |
            mlruns/
            mlflow.log
